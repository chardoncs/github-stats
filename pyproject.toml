[project]
name = "github-stats"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.0"
dependencies = [
    "aiohttp>=3.14.3",
    "requests>=2.34.2",
]
